import 'package:flutter/material.dart';

import '../../../de folos/appbar.dart';

class Health extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: barApp('Health'),
    );
  }
}
